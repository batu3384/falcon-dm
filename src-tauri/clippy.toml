# ponytail: clippy lints tuned for this project. CI runs `cargo clippy -D warnings`.
# Allow the Tauri command functions (spawned by generate_handler) to have many args.
too-many-arguments-threshold = 9
