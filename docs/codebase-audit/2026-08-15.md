# Kod tabanı denetimi — downloadmanager

- Tarih: 2026-08-15
- Kapsam: `/Users/batuhanyuksel/Documents/downloadmanager`
- Runtime: static
- Verdict: CLEAN
- Envanter: `file_count=232`, `todo_count=0` (`complete_todo_list: false`)
- Profil: `primary=typescript`, diller `{typescript:43, rust:21, javascript:10, shell:3, python:2}`, `test_kinds.jest=10`
- `workspace_markers`: `package.json`
- `entrypoints`: `src-tauri/src/main.rs`
- `secret_candidates`: `[]`
- `docs_truncated`: false
- docs-check: `md_files_scanned=36`, `link_count=4`, `broken_links=[]`, `promised_missing=[]`
- promises: `files_scanned=3`, `missing_paths=[]`, `plist_unused=[]`, `plist_missing=[]`
- import-sample: `sample: 93 edges, 58 files`, `unresolved=[]`, `cycles=[]`, `orphans_complete=true`, `truncated=false`, `complete_graph=false`
- stub-scan: `files_scanned=76`, `hit_count=0`, `complete_scan=true`
- runtime-check: `runtime: static-only`, plans `[{kind: npm-test, body: "vitest run", class: safe}]`

## Özet

| Kod | Seviye | Konu | Yol |
|-----|--------|------|-----|
| CA-001 | Minor | Maintainability | src-tauri/src/util.rs:1 |
| CA-002 | Minor | Maintainability | src-tauri/src/download/queue.rs:149 |
| CA-003 | Minor | Maintainability | src-tauri/src/lifecycle.rs:164 |
| CA-004 | Minor | Maintainability | src/components/SettingsModal.tsx:17 |
| CA-005 | Minor | Maintainability | src/types.ts |
| CA-006 | Minor | Architecture | src/lib/scheduler.ts:1 |
| CA-007 | Minor | Architecture | src/types.ts:47 |
| CA-008 | Minor | Functional | src-tauri/src/download/http.rs:33 |
| CA-009 | Minor | Security-arch | src-tauri/src/commands.rs:244 |
| CA-010 | Minor | Security-arch | src-tauri/capabilities/default.json:13 |
| CA-011 | Info | Security-arch | src-tauri/src/local_api.rs:123 |
| CA-012 | Info | Security-arch | src-tauri/src/lifecycle.rs:364 |
| CA-013 | Info | Docs | design-system/falcon-dm-v2/MASTER.md:3 |
| CA-014 | Info | Docs | .superpowers/sdd vs docs/superpowers |
| CA-015 | Info | Tests | runtime-check.py (static-only) |
| CA-016 | Info | Completeness | scripts/provision-sidecars.test.sh |
| CA-017 | Info | Architecture | extension/background.js |
| CA-018 | Info | Maintainability | src/App.tsx:45 |
| CA-019 | Info | Architecture | src/types.ts + src/lib/schema.ts |
| CA-020 | Info | Architecture | src-tauri/src/local_api.rs:78 |

Critical: 0. Major: 0. 800 satır eşiğinin üstünde kaynak yok (`top_by_lines[0]` = `util.rs` 796). Mutating `/api/add` ve `/api/intercept` `check_api_token` ile kapalı. SAST bu denetimin konusu değil — `security-check` ayrı.

## CA-001 | Minor | Maintainability | src-tauri/src/util.rs:1

**Kanıt:** inventory `top_by_lines[0]` = `{path: src-tauri/src/util.rs, lines: 796, package: src-tauri}`. Dosya HLS URL, SSRF IP, path traversal, YouTube, filename, kopya, open-path doğrulamasını tek modülde tutuyor (satır 7 `is_hls_url`, 28 `is_blocked_ip`, 108 `validate_download_url`, 796 dosya sonu test).
**Neden:** 800 eşiğinin 4 satır altında; bir helper daha Major god-file yapar. Reviewer tek dosyada tutamaz.
**Yön:** `util/net.rs` (URL/SSRF) ve `util/path.rs` (save/open/copy) ayır; `mod.rs` re-export.

## CA-002 | Minor | Maintainability | src-tauri/src/download/queue.rs:149

**Kanıt:** inventory `queue.rs` 688 satır. `tick` 149. satırda başlıyor; `DownloadRoute` match + yt-dlp/HLS/HTTP spawn + aria2 resume aynı metotta.
**Neden:** Routing helper var ama gövde hâlâ tek fonksiyon. 800’e yakın.
**Yön:** Her `DownloadRoute` kolunu ayrı `spawn_*` fonksiyonuna al; `tick` yalnız claim + dispatch.

## CA-003 | Minor | Maintainability | src-tauri/src/lifecycle.rs:164

**Kanıt:** inventory `lifecycle.rs` 669 satır. `pub fn run` (164) tray menü, Axum bind (359–414), 500ms progress poll (416+) aynı setup closure içinde.
**Neden:** Önceki god-file split `lib.rs`’i indirdi; boot orkestrasyonu hâlâ tek yığın.
**Yön:** `run_tray`, `spawn_local_api`, `spawn_progress_poll` üç çağrı.

## CA-004 | Minor | Maintainability | src/components/SettingsModal.tsx:17

**Kanıt:** inventory `SettingsModal.tsx` 609 satır (`package: "."`). General / network / profiles tek component (`activeTab` satır 21).
**Neden:** UI god-file eşiğinin altında; tab gövdeleri bağımsız değişir.
**Yön:** Tab panellerini üç dosyaya böl; modal yalnız yükle/kaydet.

## CA-005 | Minor | Maintainability | src/types.ts

**Kanıt:** import-sample `hubs[0]` = `{path: src/types.ts, in_edges: 18}`; `sample: 93 edges, 58 files`. İkinci hub `src/api/commands.ts` in_edges 12.
**Neden:** Tip + formatBytes + tema aynı barrel. Her UI değişikliği types.ts’e dokunur.
**Yön:** `DownloadModel` / `SettingsModel` ayrı; format/tema `lib/` altında. God-file ile çift sayma yok (`types.ts` 136 satır değil, hub).

## CA-006 | Minor | Architecture | src/lib/scheduler.ts:1

**Kanıt:** `isValidScheduleRange` TS (satır 1–11). Rust `validate_schedule` `queue.rs:25` (`NaiveTime`, active her iki ucu ister). Yorum: “Keep overnight-range rules aligned with validate_schedule”.
**Neden:** İki parser; TS 24:00’ü reddeder, Rust `len==5` + parse. Drift sessiz UI/backend uyumsuzluğu üretir.
**Yön:** Tek kural (Rust invoke veya paylaşılan HH:MM regex + aynı active kuralı).

## CA-007 | Minor | Architecture | src/types.ts:47

**Kanıt:** `DownloadProfile` yorumu: “Matched by url_pattern substring.” Rust `settings.rs:29` `DownloadProfile::matches` tam hostname (+ isteğe scheme/port).
**Neden:** Operatör substring sanır; `cdn.example.com` pattern’i `example.com` ile eşleşmez.
**Yön:** Yorumu hostname kuralına çek; Settings hint aynı cümleyi söylesin.

## CA-008 | Minor | Functional | src-tauri/src/download/http.rs:33

**Kanıt:** `HttpOptions { proxy, speed_limit_kbps }` — connection alanı yok. `QueueManager::set_max_connections` (`queue.rs:115`) HLS `max_connections` için. `save_settings` (`commands.rs:440`) her ikisini de yazar. Varsayılan yol HTTP tek bağlantı (DNS-pin).
**Neden:** Settings `max_connections_per_server` çoğu indirmede no-op.
**Yön:** HTTP’ye bağla (gerçek paralel) veya UI’da “yalnız HLS” de.

## CA-009 | Minor | Security-arch | src-tauri/src/commands.rs:244

**Kanıt:** `get_settings` tam `Settings` döner (`api_token` `settings.rs:69`, `download_profiles[].cookies` satır 22). `get_extension_status` yorumu satır 248: “exposes NO secret token to the frontend.” SettingsModal satır 257 token’ı `type=password` ile gösterir.
**Neden:** Pairing komutu token’ı saklar; ayarlar IPC’si aynı secret’ı webview’e koyar. XSS/devtools ile token okunur. Mutating public API değil; masaüstü webview.
**Yön:** `get_settings` token/cookies maskelesin; yazma ayrı `set_api_token` veya boş bırakınca rotate (zaten `save_settings` 444).

## CA-010 | Minor | Security-arch | src-tauri/capabilities/default.json:13

**Kanıt:** `"shell:allow-kill"` allow-list yok. `shell:allow-spawn` sidecar + arg validator (aria2c/ffmpeg).
**Neden:** Kill, spawn edilen child için gerekli; capability metni kısıtsız. Model boşluğu, PoC değil.
**Yön:** Plugin’in PID kapsamını dokümante et (SECURITY.md kısmen var) veya Tauri izin şeması daralırsa bağla.

## CA-011 | Info | Security-arch | src-tauri/src/local_api.rs:123

**Kanıt:** `handle_health` auth yok. `rate_limit_middleware` `/api/health`’i atlar (satır 38). Bind `127.0.0.1:14201` (`lifecycle.rs:397`). Extension `GET` (`shared.js:118`). Mutating değil.
**Neden:** Localhost varlık sızıntısı. Kabul edilmiş yüzey.
**Yön:** İsteğe bağlı token’lı health; zorunlu değil.

## CA-012 | Info | Security-arch | src-tauri/src/lifecycle.rs:364

**Kanıt:** CORS `AllowOrigin::predicate` her `chrome-extension://` / `moz-extension://` / `edge-extension://`. Asıl kapı `check_api_token` + `allowed_extension_ids` (`lib.rs:261`).
**Neden:** Pair öncesi origin geniş; token olmadan POST 401/403.
**Yön:** Allowlist origin’e sıkmak pair’i kırar; bırak veya pair sonrası daralt.

## CA-013 | Info | Docs | design-system/falcon-dm-v2/MASTER.md:3

**Kanıt:** inventory `docs` içinde `design-system/falcon-dm/MASTER.md` ve `falcon-dm-v2/MASTER.md`. v1 “Superseded by v2”. v2 satır 3: `design-system/falcon-dm/pages/[page-name].md` — glob `design-system/**/pages/**` boş.
**Neden:** İki MASTER; v2 olmayan path’e işaret.
**Yön:** v1’i arşiv klasörüne al; v2 path’i gerçek pages’e veya cümleyi sil.

## CA-014 | Info | Docs | .superpowers/sdd vs docs/superpowers

**Kanıt:** inventory `docs_count=36`, `docs_truncated=false`. `.superpowers/sdd/implementation_plan/*` (task 1–12 complete) + `docs/superpowers/plans|specs/2026-08-09-falcon-dm-hardening-and-ux*.md`.
**Neden:** İki SDD ağacı; tarihçe gürültüsü.
**Yön:** Tek kanon (`docs/superpowers`); `.superpowers/sdd` arşiv.

## CA-015 | Info | Tests | runtime-check.py (static-only)

**Kanıt:** `runtime: static-only`. Plan tek: `npm-test` / `vitest run` / `class: safe`. `cargo test` CI `rust` job (`.github/workflows/ci.yml:71`) ve `package.json` `test:rust` içinde; runtime-check `package.json` `test` script’ine bakıyor. Bu tur `--runtime` yok.
**Neden:** CLEAN ≠ test koştu. Cargo bu scriptin plan listesinde yok.
**Yön:** İsteğe bağlı `/codebase-audit --runtime`; cargo’yu ayrı plan saymak script işi.

## CA-016 | Info | Completeness | scripts/provision-sidecars.test.sh

**Kanıt:** inventory `profile.orphan_tests[0]` = `{path: scripts/provision-sidecars.test.sh, expected_stem: provision-sidecars}`. Kardeş `provision-sidecars.sh` var; isim `*.test.sh`.
**Neden:** İsim uyuşmazlığı gürültüsü, eksik ürün değil. CI satır 57 çalıştırıyor.
**Yön:** Yoksay veya `provision-sidecars.sh.test` convention notu.

## CA-017 | Info | Architecture | extension/background.js

**Kanıt:** import-sample `orphans` içinde `extension/background.js`, `content.js`, `shared.js`, `pairing.js`, `api.js`, `popup.js`, `options.js`, `media-utils.js`. `orphans_complete: true`. inventory `entrypoints` yalnız `src-tauri/src/main.rs`. Manifest MV3 service worker relative-import grafiğine girmez.
**Neden:** Ölü kod değil; örnekleyici MV3’ü entry saymıyor.
**Yön:** Finding değil, ölçüm sınırı. `complete_graph: false` — “repoda cycle yok” iddiası yok.

## CA-018 | Info | Maintainability | src/App.tsx:45

**Kanıt:** inventory `App.tsx` 556 satır. Clipboard `useClipboardMonitor` (satır 43). App hâlâ modal/palette/theme/toolbar orkestrasyonu.
**Neden:** Domain kancası çıktı; shell hâlâ şişman. 800 altı.
**Yön:** İsteğe bağlı shell/layout split; zorunlu değil.

## CA-019 | Info | Architecture | src/types.ts + src/lib/schema.ts

**Kanıt:** `DownloadStatus` / `DownloadModel` `types.ts:4–28`; aynı alanlar zod `schema.ts:10–47`. `commands.ts` parse edip `as DownloadModel` basıyor.
**Neden:** Üçüncü kopya Rust `storage/models`. Drift riski; şu an hizalı.
**Yön:** Zod’dan tip üret (`z.infer`) veya tek kaynak.

## CA-020 | Info | Architecture | src-tauri/src/local_api.rs:78

**Kanıt:** `enqueue` hatasında `Ok(Json({success:false, error}))` — HTTP 200. Extension `api.js:70` `data.success === false` bakıyor.
**Neden:** HTTP katmanı her zaman 200; izleme/araçlar hata görmez.
**Yön:** 4xx map veya bilinçli bırak (mevcut client sözleşmesi).

## Önerilen sıra

1. CA-001 — `util.rs` net/path diye böl; 800 eşiğini geçirme.
2. CA-002 — `tick` gövdesini route spawn helper’larına indir.
3. CA-009 — `get_settings` token/cookies maskelesin; yazma ayrı kalsın.
4. CA-008 — `max_connections_per_server` HTTP’ye bağlansın veya HLS-only işaretlensin.
5. CA-007 — profil eşleşme yorumunu Rust hostname kuralına çek.

SAST / CWE tarama bu raporun dışında. `security-check` ayrı.
